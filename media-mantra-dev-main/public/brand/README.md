# Brand assets

- **`mm-global-lockup.png`** — approved **full colour lockup** on white (684×374 — mm mark, wordmark, “Building Your Story”). Used site‑wide via `MediaMantraLogo`.
- **`brand-guide-print-environment.png`** — source page from the identity manual (reference).
- **`mm-mark.svg`** — legacy vector mm symbol only (optional decks/email).

Header/footer render the lockup inside a white rounded badge; the image fills the frame without cropping.
